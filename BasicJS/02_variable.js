const name="sandhya";
var acc=12223;
let course="b.tech";
//name="sanu";//once is declare can not change
console.log(name);
//var should never be used because of scop of function
//always use let for variable declare
num=34;
let sum;
console.table([name,course,acc,num,sum]);
console.log(typeof(num));
console.log(sum);